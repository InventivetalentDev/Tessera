{
  "name": "tessera-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Documentation site for the Tessera Minecraft plugin.",
  "scripts": {
    "dev": "vitepress dev src",
    "build": "vitepress build src",
    "preview": "vitepress preview src",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "wrangler": "^4.0.0"
  }
}
